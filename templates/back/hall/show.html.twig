{% extends 'back/base.html.twig' %}

{% block title %}Hall{% endblock %}

{% block body %}
    <h1 id="h1">Hall</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hall.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hall.name }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ hall.location }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ hall.capacity }}</td>
            </tr>
        </tbody>
    </table>

 <div id="c"><a href="{{ path('back_hall_index') }}">Retour</a>

    <a href="{{ path('back_hall_edit', {'id': hall.id}) }}">Modifier</a>

    {{ include('back/hall/_delete_form.html.twig') }}
</div>
{% endblock %}
