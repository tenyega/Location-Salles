{% extends 'back/base.html.twig' %}

{% block title %}GESTION DE TABLE  {% endblock %}

{% block body %}
    <h1 id="h1">GESTION DE TABLE - LOGIIEL</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>NOM</th>
                           
                <th>ACTIONS</th>
            </tr>
        </thead>
        <tbody>
        {% for software in softwares %}
            <tr>
                <td>{{ software.id }}</td>
                <td>{{ software.name }}</td>
               
                <td>
                    <a href="{{ path('back_software_editSoftware', {'id' : software.id }) }}">MODIFIER</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">PAS DE DONNEE TROUVE</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div id="c"><a href="{{ path('back_software_newSoftware')}}">AJOUTER LOGICIEL</a></div>
    <div id="c"><a href="{{ path('back_halltable_gestiontable') }}">Retour</a></div>


{% endblock %}
