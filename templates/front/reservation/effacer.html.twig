<form method="post" action="{{ path('back_reservation_effacer', {'id': reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
   <button id="btn">Effacer</button>
</form>